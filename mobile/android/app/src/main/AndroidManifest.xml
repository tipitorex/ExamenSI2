<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- ============================================ -->
    <!-- PERMISOS NECESARIOS PARA LA APP              -->
    <!-- ============================================ -->
    
    <!-- Internet (para llamadas API) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Cámara (para tomar fotos del vehículo) -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Micrófono (para grabar audio describiendo el problema) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- Ubicación precisa (GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- Ubicación aproximada (red/wifi) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- ============================================ -->
    <!-- CONFIGURACIÓN DE LA APP                      -->
    <!-- ============================================ -->
    
    <application
        android:label="CeroEspera"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <!-- Necesario para que funcione el procesamiento de texto en Android -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>